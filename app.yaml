runtime: python312

entrypoint: gunicorn -b :$PORT posvendas.wsgi

beta_settings:
  cloud_sql_instances: posvendas-rj:southamerica-east1:yosefposvendas

handlers:
  - url: /static
    static_dir: static/
    secure: always

  - url: /.*
    script: auto
    secure: always

env_variables:
  DJANGO_SETTINGS_MODULE: "posvendas.settings"
  SECRET_KEY: "django-insecure-g(=+cz%1!b$vwi7m67(k_9@&wm22^+s%xj#*2oyuetvjt7!9wn"
  DB_NAME: config('DB_NAME')
  DB_USER: config('DB_USER')
  DB_PASSWORD: config('DB_PASSWORD')
  DB_PORT: config('DB_PORT', default='')
  DB_HOST: config('DB_HOST')
  ALLOWED_HOSTS: config('ALLOWED_HOSTS')

automatic_scaling:
  max_instances: 2